{{--
    The panel design system is compiled from
    resources/css/filament/control/theme.css.

    This hook remains intentionally empty so every surface is sourced from one
    versioned stylesheet rather than view-local style blocks. It also gives us
    a stable extension point for panel-safe runtime markup in the future.
--}}
