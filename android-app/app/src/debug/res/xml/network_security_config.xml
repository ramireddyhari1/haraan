<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY. The app ships with usesCleartextTraffic="false" (prod is
  HTTPS), which also blocks http://127.0.0.1:8000 — so a debug build could not
  reach `php artisan serve` at all and every local request failed silently as an
  empty/timed-out response.

  This permits cleartext to the loopback hosts ONLY:
    - 127.0.0.1  reached over `adb reverse tcp:8000 tcp:8000` (emulator + device)
    - 10.0.2.2   the emulator's alias for the host machine
  Everything else, including prod, still requires TLS. Release builds never see
  this file — it lives in src/debug.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
